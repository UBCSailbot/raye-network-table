syntax = "proto2";

package NetworkTable;

import "GetValuesReply.proto";

message Reply {
    required Type type = 1;

    optional GetValuesReply getvalues_reply = 2;

    enum Type {
        GETVALUES = 0;
    }
}
